name: dart_counter
description: A simple dart counter application
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  web: ^0.5.0

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^3.0.0 